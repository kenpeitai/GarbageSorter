pluginManagement {
    plugins {
        id("com.android.application") version '8.11.1'
    }
    repositories {
        google()
        gradlePluginPortal()

        mavenCentral()

    }

}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {

        google()
        gradlePluginPortal()

        mavenCentral()
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Garbage Sorter"
include ':app'
